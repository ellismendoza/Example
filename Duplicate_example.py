from common_functions import *

# Uses functions for area, perimeter, volume, and surface_area
area = calculate_area(6, 4)
perimeter = calculate_perimeter(6, 4)
volume = calculate_volume(8, 4, 2)
surface_area = calculate_surface_area(8, 4, 2)

# Uses functions for calculating area and perimeter at once
area, perimeter = calculate_area_perimeter(6, 4)
